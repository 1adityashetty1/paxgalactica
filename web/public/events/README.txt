Art for action outcomes — see docs/todo.md item 17.

Only `negotiation` is a file now. `refusal` and `defiance` are drawn as pixel
art in `src/ui/outcomeart.ts` and rendered as SVG by `OutcomeArt.tsx` — they
were illustrations in a contemporary corporate register against a game made of
pixel sprites, and the defiance one was about a polling collapse rather than a
leader overruling their own institutions. They are now one door in two states,
which is a thing two separate pictures cannot be.

  negotiation.jpeg  it needs another power to agree

16:9, resized like the portraits (sources in the gitignored faction_portraits/):

  sips -Z 1000 --setProperty formatOptions 75 <source> --out web/public/events/<name>.jpeg

A missing file must fall back to the text treatment, never to a hole.
